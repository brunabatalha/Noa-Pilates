import React from "react";
import ReactDOM from "react-dom/client";
import NoaPilates from "./NoaPilates.jsx";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <NoaPilates />
  </React.StrictMode>
);
